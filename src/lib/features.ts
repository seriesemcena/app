/**
 * Release gates for features that must stay unavailable in the public app.
 * PRO membership remains assignable only by the trusted admin API.
 */
export const PRO_SELF_SERVICE_ENABLED = false;
export const AI_CURATION_ENABLED = false;
